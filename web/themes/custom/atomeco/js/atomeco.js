/**
 * @file
 * atomeco behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.atomeco = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
